import { OverridableComponent } from '@mui/material/node_modules/@mui/types';
import { BoxTypeMap } from '@mui/material/node_modules/@mui/system/Box';
import { Theme as SystemTheme } from '@mui/material/node_modules/@mui/system/createTheme';

export default function createBox<
  T extends object = SystemTheme,
  AdditionalProps extends Record<string, unknown> = {},
>(options?: {
  themeId?: string;
  defaultTheme: T;
  defaultClassName?: string;
  generateClassName?: (componentName: string) => string;
}): OverridableComponent<BoxTypeMap<AdditionalProps, 'div', T>>;
