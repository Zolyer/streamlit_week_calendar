import * as React from 'react';
import { Interpolation } from '@mui/material/node_modules/@mui/system/node_modules/@mui/styled-engine';
import { Theme as SystemTheme } from '@mui/material/node_modules/@mui/system/createTheme';
export interface GlobalStylesProps<Theme = SystemTheme> {
    styles: Interpolation<Theme>;
    defaultTheme?: object;
    themeId?: string;
}
declare function GlobalStyles<Theme = SystemTheme>({ styles, themeId, defaultTheme, }: GlobalStylesProps<Theme>): React.JSX.Element;
declare namespace GlobalStyles {
    var propTypes: any;
}
export default GlobalStyles;
